<nav class="border-b border-slate-800/80 bg-slate-950/80 backdrop-blur sticky top-0 z-50">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 py-4 flex flex-wrap items-center justify-between gap-3">
    <a href="/" class="flex items-center gap-2 font-bold text-xl">
      <span class="text-amber-400">⚡</span>
      <span>Bot<span class="text-amber-400">God</span></span>
      <span class="hidden sm:inline text-xs font-normal text-slate-500 ml-1">.pro</span>
    </a>
    <div class="flex flex-wrap items-center gap-4 text-sm">
      <a href="/features" class="text-slate-300 hover:text-amber-400 transition">Features</a>
      <a href="/status" class="text-slate-300 hover:text-amber-400 transition">Status</a>
      <% if (typeof inviteUrl !== 'undefined' && inviteUrl) { %>
        <a href="<%= inviteUrl %>" class="text-slate-300 hover:text-amber-400 transition" target="_blank" rel="noopener">Invitar</a>
      <% } %>
      <% if (typeof user !== 'undefined' && user) { %>
        <a href="/servers" class="text-slate-300 hover:text-amber-400 transition">Dashboard</a>
        <a href="/logout" class="text-red-400 hover:text-red-300 transition">Salir</a>
      <% } else { %>
        <a href="/login" class="bg-indigo-600 hover:bg-indigo-500 text-white font-semibold px-4 py-2 rounded-lg transition">Login</a>
      <% } %>
    </div>
  </div>
</nav>
