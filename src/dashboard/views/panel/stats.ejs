<div class="space-y-6 max-w-4xl">
  <div class="dash-hero">
    <div class="relative z-10">
      <p class="text-blue-400/80 text-xs font-bold tracking-[0.2em] uppercase mb-2">Contadores</p>
      <h1 class="text-2xl sm:text-3xl font-black text-white mb-1">📊 Estadísticas</h1>
      <p class="text-slate-500 text-sm">Canales de voz con nombre dinámico (miembros, online, boosts).</p>
    </div>
  </div>
  <div class="dash-card p-6">
    <form id="statsForm" class="space-y-5">
      <label class="flex items-center gap-3 p-3 rounded-lg bg-white/[0.02] border border-white/5 cursor-pointer">
        <input type="checkbox" name="stats_enabled" <%= modules.find(m=>m.name==='stats')?.enabled ? 'checked' : '' %> class="rounded accent-amber-400"/>
        <span class="font-semibold text-slate-200">Activar Stats dinámicos</span>
      </label>
      <div class="grid sm:grid-cols-3 gap-4">
        <div><label class="block text-xs text-slate-500 mb-1.5">Canal miembros</label><%- channelSelect('statsChannels.members', settings.statsChannels?.members) %></div>
        <div><label class="block text-xs text-slate-500 mb-1.5">Canal en línea</label><%- channelSelect('statsChannels.online', settings.statsChannels?.online) %></div>
        <div><label class="block text-xs text-slate-500 mb-1.5">Canal boosts</label><%- channelSelect('statsChannels.boosts', settings.statsChannels?.boosts) %></div>
      </div>
      <button type="submit" class="dash-btn-primary">Guardar</button>
    </form>
  </div>
</div>
<script>
document.getElementById('statsForm').addEventListener('submit', async (e) => {
  e.preventDefault();
  const fd = new FormData(e.target);
  await saveSettings('/api/guilds/<%= guild.id %>/settings', {
    statsChannels: {
      members: fd.get('statsChannels.members') || null,
      online: fd.get('statsChannels.online') || null,
      boosts: fd.get('statsChannels.boosts') || null,
    },
  });
  await saveSettings('/api/guilds/<%= guild.id %>/modules', { modules: { stats: fd.get('stats_enabled') === 'on' } });
});
</script>